{
  "mcpServers": {
    "dripage": {
      "description": "Dripage browser automation and vision analysis",
      "command": "python",
      "args": [
        "mcp_server.py"
      ],
      "cwd": ".",
      "env": {
        "PYTHONPATH": "."
      }
    }
  },
  "imports": ["cursor", "claude-code", "claude-desktop", "opencode", "vscode"]
}
